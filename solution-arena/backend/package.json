{
  "name": "tech-sales-training-backend",
  "version": "2.0.0",
  "description": "Backend API for IBM Tech Sales Training Platform",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "db:setup": "psql -U postgres -c 'CREATE DATABASE dealsprint;' && npm run db:schema && npm run db:seed",
    "db:schema": "psql -U postgres -d dealsprint -f ../database/schema.sql",
    "db:seed": "node ../database/seed.js",
    "db:reset": "psql -U postgres -c 'DROP DATABASE IF EXISTS dealsprint;' && npm run db:setup",
    "test": "jest --coverage"
  },
  "keywords": [
    "ibm",
    "sales",
    "training",
    "api"
  ],
  "author": "IBM",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}